const base = require('./base.svg');
const heart = require('./heart.svg');
const cart = require('./cart.svg');
const user = require('./user.svg');
const logout = require('./logout.svg');

const iconsCollection = { base, heart, cart, user, logout };

export default iconsCollection;
